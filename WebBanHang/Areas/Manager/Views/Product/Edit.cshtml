@model WebBanHang.ViewModels.UpdateProductVM

@{
    ViewData["Title"] = "Edit Product";
}

<div class="container-fluid px-4">
    <h1 class="mt-4">Edit Product</h1>
    <ol class="breadcrumb mb-4">
        <li class="breadcrumb-item"><a asp-area="Manager" asp-controller="Product" asp-action="Index">Products</a></li>
        <li class="breadcrumb-item active">Edit</li>
    </ol>

    <div class="card mb-4">
        <div class="card-header">
            <i class="fas fa-edit me-1"></i>
            Edit Product: @Model.Name
        </div>
        <div class="card-body">
            <form asp-action="Edit" method="post" enctype="multipart/form-data">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <input type="hidden" asp-for="Id" />
                <!-- Hidden fields to preserve data not in form if needed, though mostly handled by controller logic or optional -->
                <input type="hidden" asp-for="CreatedDate" />
                <input type="hidden" asp-for="CreatedBy" />

                <div class="row">
                    <!-- Left Column -->
                    <div class="col-md-6">
                        <div class="form-group mb-3">
                            <label asp-for="Code" class="control-label fw-bold">Code</label>
                            <input asp-for="Code" class="form-control" />
                            <span asp-validation-for="Code" class="text-danger"></span>
                        </div>

                        <div class="form-group mb-3">
                            <label asp-for="Name" class="control-label fw-bold">Product Name</label>
                            <input asp-for="Name" class="form-control" />
                            <span asp-validation-for="Name" class="text-danger"></span>
                        </div>

                        <div class="form-group mb-3">
                            <label asp-for="CategoryId" class="control-label fw-bold">Category</label>
                            <select asp-for="CategoryId" class="form-select">
                                @foreach (var item in Model.Categories)
                                {
                                    <option value="@item.CategoryId">@item.Name</option>
                                }
                            </select>
                            <span asp-validation-for="CategoryId" class="text-danger"></span>
                        </div>

                        <div class="form-group mb-3">
                            <label class="control-label fw-bold">Current Image</label>
                            <div class="mb-2">
                                @if (Model.ExistingImages != null && Model.ExistingImages.Any(p => p.IsPrimary == true))
                                {
                                    <img src="@Model.ExistingImages.First(p => p.IsPrimary == true).ImageUrl" style="width: 100px; height: 100px; object-fit: cover;" class="img-thumbnail" />
                                    <!-- Logic to delete old image could be added here -->
                                }
                                else
                                {
                                    <span class="text-muted fst-italic">No image available</span>
                                }
                            </div>
                            <label class="control-label">Update Image (New)</label>
                            <input type="file" asp-for="ImageFile" class="form-control" />
                          
                        </div>
                    </div>

                    <!-- Right Column -->
                    <div class="col-md-6">
                        <div class="form-group mb-3">
                            <label asp-for="SellingPrice" class="control-label fw-bold">Price (VNĐ)</label>
                            <input asp-for="SellingPrice" class="form-control" type="number" step="0.01" />
                            <span asp-validation-for="SellingPrice" class="text-danger"></span>
                        </div>

                        <div class="form-group mb-3">
                            <label asp-for="ImportPrice" class="control-label fw-bold">Import Price (VNĐ)</label>
                            <input asp-for="ImportPrice" class="form-control" type="number" step="0.01" />
                            <span asp-validation-for="ImportPrice" class="text-danger"></span>
                        </div>

                        <div class="form-group mb-3">
                            <label asp-for="StockQuantity" class="control-label fw-bold">Stock Quantity</label>
                            <input asp-for="StockQuantity" class="form-control" type="number" />
                            <span asp-validation-for="StockQuantity" class="text-danger"></span>
                        </div>

                        <div class="form-group mb-3">
                            <label asp-for="Status" class="control-label fw-bold">Status</label>
                            <select asp-for="Status" class="form-select">
                                <option value="Active">Active</option>
                                <option value="Inactive">Inactive</option>

                            </select>
                            <span asp-validation-for="Status" class="text-danger"></span>
                        </div>

                        <div class="form-group mb-3">
                            <label asp-for="SupplierName" class="control-label fw-bold">Supplier Name</label>
                            <input asp-for="SupplierName" class="form-control" maxlength="255" />
                            <span asp-validation-for="SupplierName" class="text-danger"></span>
                        </div>
                    </div>

                    <div class="col-12">
                        <div class="form-group mb-3">
                            <label asp-for="Description" class="control-label fw-bold">Description</label>
                            <textarea asp-for="Description" class="form-control" rows="4"></textarea>
                            <span asp-validation-for="Description" class="text-danger"></span>
                        </div>
                    </div>
                </div>

                <div class="form-group mt-3 text-end">
                    <a asp-action="Index" class="btn btn-secondary me-2">Cancel</a>
                    <input type="submit" value="Save Changes" class="btn btn-primary" />
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
